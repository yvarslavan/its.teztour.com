stages:
  - validate
  - test
  - build
  - pre-deploy
  - deploy
  - post-deploy
  - rollback

variables:
  DEPLOY_PATH: "/opt/www"
  SERVICE_NAME: "flask-helpdesk"
  BACKUP_PATH: "/opt/backups/flask_helpdesk"
  SOCKET_PATH: "/run/gunicorn/gunicorn.sock"
  PYTHON_VERSION: "3.11"
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"
  AFTER_SCRIPT_IGNORE_ERRORS: false
  DEPLOY_DOMAIN: "its.tez-tour.com"

cache:
  paths:
    - .cache/pip/

# ============ ЭТАП 1: ВАЛИДАЦИЯ ============
validate_syntax:
  stage: validate
  image: python:3.9-slim
  tags:
    - docker
    - linux
  script:
    - echo "🔍 Проверка синтаксиса Python (Shared Runner)..."
    - python -m py_compile app.py
    - echo "✅ Синтаксис корректен"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_REF_NAME == "main"'

# ============ ЭТАП 2: ТЕСТИРОВАНИЕ ============
run_tests:
  stage: test
  image: python:3.9-slim
  tags:
    - docker
    - linux
  before_script:
    - apt-get update -qq && apt-get install -y -qq git
    - pip install --upgrade pip
    - pip install -r requirements.txt
  script:
    - echo "🧪 Запуск тестов (Shared Runner)..."
    - python -m pytest tests/ -v || echo "⚠️ Тесты не найдены или завершились с ошибками"
    - echo "✅ Тестирование завершено"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_REF_NAME == "main"'
  allow_failure: true

# ============ ЭТАП 3: СБОРКА ============
build_deployment_package:
  stage: build
  image: alpine:latest
  tags:
    - docker
    - linux
  before_script:
    - apk add --no-cache tar gzip
  script:
    - echo "📦 Создание пакета для деплоя (Shared Runner)..."
    - mkdir -p deployment_package
    - cp -r blog/ deployment_package/ 2>/dev/null || echo "⚠️ Директория blog не найдена"
    - cp -r static/ deployment_package/ 2>/dev/null || echo "⚠️ Директория static не найдена"
    - cp -r templates/ deployment_package/ 2>/dev/null || echo "⚠️ Директория templates не найдена"
    - cp -r migrations/ deployment_package/ 2>/dev/null || echo "⚠️ Директория migrations не найдена"
    - cp app.py deployment_package/ 2>/dev/null || echo "⚠️ Файл app.py не найден"
    - cp requirements.txt deployment_package/ 2>/dev/null || echo "⚠️ Файл requirements.txt не найден"
    - cp flask-helpdesk.service.redhat deployment_package/flask-helpdesk.service 2>/dev/null || echo "⚠️ Файл flask-helpdesk.service.redhat не найден"
    - cp flask-helpdesk.nginx.conf deployment_package/ 2>/dev/null || echo "⚠️ Файл flask-helpdesk.nginx.conf не найден"
    - cp config.ini deployment_package/ 2>/dev/null || echo "⚠️ Файл config.ini не найден"
    - cp *.py deployment_package/ 2>/dev/null || echo "⚠️ Дополнительные .py файлы не найдены"
    - cd deployment_package && tar -czf ../deployment_package.tar.gz . && cd ..
    - echo "📦 Полное содержимое архива deployment_package.tar.gz:"
    - tar -tvf deployment_package.tar.gz | head -n 50 || true
    - echo "✅ Пакет для деплоя создан deployment_package.tar.gz"
    - echo "📏 Размер пакета $(du -h deployment_package.tar.gz | cut -f1)"
  artifacts:
    paths:
      - deployment_package.tar.gz
      - deployment_package/
    expire_in: 1 day
  rules:
    - if: '$CI_COMMIT_REF_NAME == "main"'

# ============ ЭТАП 4: ПРЕДВАРИТЕЛЬНЫЕ ПРОВЕРКИ ============
pre_deploy_checks:
  stage: pre-deploy
  image: alpine:latest
  tags:
    - docker
    - linux
  before_script:
    - apk add --no-cache openssh-client
  script:
    - |
      # Проверяем наличие необходимых переменных
      echo "🔍 Проверка переменных окружения для Red Hat сервера (Shared Runner)..."
      if [ -z "$SSH_PRIVATE_KEY" ] || [ -z "$DEPLOY_SERVER" ] || [ -z "$DEPLOY_USER" ]; then
        echo "ℹ️ Переменные деплоя не настроены - пропускаем проверки"
        echo "✅ Для настройки добавьте SSH_PRIVATE_KEY, DEPLOY_SERVER, DEPLOY_USER в GitLab CI/CD Settings"
        exit 0
      fi

      echo "✅ Все переменные деплоя установлены"

      # Настраиваем SSH
      eval $(ssh-agent -s)
      echo "🔐 Настройка SSH ключа..."

      # Создаем временный файл для SSH ключа
      SSH_KEY_CONTENT=$(mktemp)

      if echo "$SSH_PRIVATE_KEY" | head -c 10 | grep -q "^-----BEGIN"; then
        echo "🔑 Обычный SSH ключ"
        printf '%s\n' "$SSH_PRIVATE_KEY" > "$SSH_KEY_CONTENT"
      else
        echo "🔐 Base64 кодированный SSH ключ, декодируем"
        echo "$SSH_PRIVATE_KEY" | base64 -d > "$SSH_KEY_CONTENT"
      fi

      chmod 600 "$SSH_KEY_CONTENT"
      ssh-add "$SSH_KEY_CONTENT"
      rm -f "$SSH_KEY_CONTENT"

      mkdir -p ~/.ssh
      chmod 700 ~/.ssh
      ssh-keyscan -H $DEPLOY_SERVER >> ~/.ssh/known_hosts

      echo "🔍 Предварительные проверки Red Hat сервера..."

      # Проверка SSH подключения
      if ! ssh $DEPLOY_USER@$DEPLOY_SERVER "echo 'SSH соединение OK'"; then
        echo "❌ Ошибка SSH подключения"
        exit 1
      fi

      echo "✅ SSH проверка пройдена"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "main"'
      when: always

# ============ ЭТАП 5: ДЕПЛОЙ НА RED HAT ============
deploy_to_redhat_server:
  stage: deploy
  image: alpine:latest
  tags:
    - docker
    - linux
  before_script:
    - apk add --no-cache openssh-client rsync
  script:
    - |
      # Проверяем наличие необходимых переменных
      echo "🔍 Проверка переменных окружения для деплоя (Shared Runner)..."
      if [ -z "$SSH_PRIVATE_KEY" ] || [ -z "$DEPLOY_SERVER" ] || [ -z "$DEPLOY_USER" ]; then
        echo "ℹ️ Переменные деплоя не настроены - пропускаем деплой"
        echo "✅ Для настройки добавьте SSH_PRIVATE_KEY, DEPLOY_SERVER, DEPLOY_USER в GitLab CI/CD Settings"
        exit 0
      fi

      echo "✅ Все переменные деплоя установлены"

      # Настраиваем SSH
      eval $(ssh-agent -s)
      echo "🔐 Настройка SSH ключа..."

      SSH_KEY_CONTENT=$(mktemp)

      if echo "$SSH_PRIVATE_KEY" | head -c 10 | grep -q "^-----BEGIN"; then
        echo "🔑 Обычный SSH ключ"
        printf '%s\n' "$SSH_PRIVATE_KEY" > "$SSH_KEY_CONTENT"
      else
        echo "🔐 Base64 кодированный SSH ключ, декодируем"
        echo "$SSH_PRIVATE_KEY" | base64 -d > "$SSH_KEY_CONTENT"
      fi

      chmod 600 "$SSH_KEY_CONTENT"
      ssh-add "$SSH_KEY_CONTENT"
      rm -f "$SSH_KEY_CONTENT"

      mkdir -p ~/.ssh
      chmod 700 ~/.ssh
      ssh-keyscan -H $DEPLOY_SERVER >> ~/.ssh/known_hosts

      echo "🚀 Начинаем деплой на Red Hat сервер (Shared Runner)..."

      # Устанавливаем переменные по умолчанию
      DEPLOY_PATH=${DEPLOY_PATH:-/opt/www}
      BACKUP_PATH=${BACKUP_PATH:-/opt/backups/flask_helpdesk}
      SERVICE_NAME=${SERVICE_NAME:-flask-helpdesk}

      # Создаем бэкап существующего проекта
      ssh $DEPLOY_USER@$DEPLOY_SERVER "
        echo '💾 Создаем бэкап...'
        if [ -d '$DEPLOY_PATH' ]; then
          sudo mkdir -p $BACKUP_PATH
          sudo cp -r $DEPLOY_PATH $BACKUP_PATH/backup_\$(date +%Y%m%d_%H%M%S)
          echo '✅ Бэкап создан'
        fi
      "

      # Останавливаем сервис
      ssh $DEPLOY_USER@$DEPLOY_SERVER "
        echo '⏹️ Останавливаем сервис на Red Hat...'
        if sudo systemctl is-active --quiet $SERVICE_NAME; then
          sudo systemctl stop $SERVICE_NAME
          echo '✅ Сервис остановлен'
        else
          echo 'ℹ️ Сервис уже остановлен'
        fi
      "

      # Подготавливаем директории
      ssh $DEPLOY_USER@$DEPLOY_SERVER "
        echo '🔧 Подготовка директорий для Red Hat...'
        sudo mkdir -p $DEPLOY_PATH
        sudo chown -R $DEPLOY_USER:$DEPLOY_USER $DEPLOY_PATH
        sudo chmod -R 755 $DEPLOY_PATH
      "

      # Синхронизируем файлы
      echo '📁 Синхронизируем файлы на Red Hat сервер...'
      if [ -f "deployment_package.tar.gz" ]; then
        echo '🗜️ Передаём архив и распаковываем на Red Hat сервер'

        cat deployment_package.tar.gz | ssh $DEPLOY_USER@$DEPLOY_SERVER '
          set -e
          RELEASE_DIR=$(mktemp -d)
          echo "Распаковка архива во временную директорию $RELEASE_DIR на Red Hat..."
          sudo tar -xzf - -C "$RELEASE_DIR"

          echo "Синхронизируем файлы в $DEPLOY_PATH..."
          sudo cp -r "$RELEASE_DIR"/* "$DEPLOY_PATH/"

          echo "Очистка временных файлов..."
          sudo rm -rf "$RELEASE_DIR"
        '

        echo '✅ Архив передан и синхронизирован на Red Hat'
      else
        echo '❌ Архив deployment_package.tar.gz не найден'
        exit 1
      fi

      echo "🎉 Базовый деплой файлов завершен успешно!"
      echo "🌐 Далее нужно настроить сервис и запустить приложение на сервере"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "main"'
      when: always

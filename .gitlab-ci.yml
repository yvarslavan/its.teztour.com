stages:
  - validate
  - test
  - build
  - deploy

variables:
  DEPLOY_PATH: "/opt/www"
  SERVICE_NAME: "flask-helpdesk"
  BACKUP_PATH: "/opt/backups/flask_helpdesk"
  DEPLOY_DOMAIN: "its.tez-tour.com"

# ============ ЭТАП 1: ВАЛИДАЦИЯ (БЕЗ ТЕГОВ) ============
validate_syntax:
  stage: validate
  image: python:3.9-slim
  script:
    - echo "🔍 Проверка синтаксиса Python..."
    - python -m py_compile app.py || echo "⚠️ app.py не найден или есть ошибки синтаксиса"
    - echo "✅ Валидация завершена"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "main"'
  allow_failure: true

# ============ ЭТАП 2: ПРОСТОЕ ТЕСТИРОВАНИЕ ============
run_basic_tests:
  stage: test
  image: python:3.9-slim
  before_script:
    - apt-get update -qq && apt-get install -y -qq git
    - pip install --upgrade pip
  script:
    - echo "🧪 Базовая проверка requirements.txt..."
    - pip install -r requirements.txt || echo "⚠️ Проблемы с requirements.txt"
    - echo "✅ Базовое тестирование завершено"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "main"'
  allow_failure: true

# ============ ЭТАП 3: СОЗДАНИЕ ПАКЕТА ============
build_package:
  stage: build
  image: alpine:latest
  before_script:
    - apk add --no-cache tar gzip
  script:
    - echo "📦 Создание простого пакета для деплоя..."
    - mkdir -p deployment_package
    - echo "Копируем основные файлы"
    - cp app.py deployment_package/ 2>/dev/null || echo "⚠️ app.py не найден"
    - cp requirements.txt deployment_package/ 2>/dev/null || echo "⚠️ requirements.txt не найден"
    - cp config.ini deployment_package/ 2>/dev/null || echo "⚠️ config.ini не найден"
    - cp *.py deployment_package/ 2>/dev/null || echo "⚠️ Дополнительные .py файлы не найдены"
    - echo "Копируем директории если есть"
    - cp -r blog/ deployment_package/ 2>/dev/null || echo "ℹ️ blog/ не найдена"
    - cp -r static/ deployment_package/ 2>/dev/null || echo "ℹ️ static/ не найдена"
    - cp -r templates/ deployment_package/ 2>/dev/null || echo "ℹ️ templates/ не найдена"
    - cp -r migrations/ deployment_package/ 2>/dev/null || echo "ℹ️ migrations/ не найдена"
    - echo "Копируем конфигурационные файлы для сервера"
    - cp flask-helpdesk.service.redhat deployment_package/flask-helpdesk.service 2>/dev/null || echo "ℹ️ flask-helpdesk.service.redhat не найден"
    - cp flask-helpdesk.nginx.conf deployment_package/ 2>/dev/null || echo "ℹ️ flask-helpdesk.nginx.conf не найден"
    - echo "Создаем архив"
    - cd deployment_package && tar -czf ../deployment_package.tar.gz . && cd ..
    - echo "📦 Содержимое пакета:"
    - tar -tvf deployment_package.tar.gz | head -n 20
    - echo "✅ Пакет создан"
    - du -h deployment_package.tar.gz
  artifacts:
    paths:
      - deployment_package.tar.gz
    expire_in: 1 day
  rules:
    - if: '$CI_COMMIT_REF_NAME == "main"'

# ============ ЭТАП 4: ДЕПЛОЙ (УПРОЩЕННЫЙ) ============
deploy_to_server:
  stage: deploy
  image: alpine:latest
  before_script:
    - apk add --no-cache openssh-client
  script:
    - echo "🚀 Начинаем упрощенный деплой..."
    - |
      if [ -z "$SSH_PRIVATE_KEY" ] || [ -z "$DEPLOY_SERVER" ] || [ -z "$DEPLOY_USER" ]; then
        echo "ℹ️ Переменные деплоя не настроены"
        echo "📋 Необходимые переменные в GitLab CI/CD Settings:"
        echo "   - SSH_PRIVATE_KEY (приватный SSH ключ)"
        echo "   - DEPLOY_SERVER (IP сервера: 10.7.74.252)"
        echo "   - DEPLOY_USER (пользователь: deploy)"
        echo "✅ Пакет готов к деплою"
        exit 0
      fi
    - echo "✅ Переменные деплоя найдены"
    - eval $(ssh-agent -s)
    - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -
    - mkdir -p ~/.ssh
    - chmod 700 ~/.ssh
    - ssh-keyscan -H $DEPLOY_SERVER >> ~/.ssh/known_hosts
    - echo "🔍 Проверка подключения к серверу..."
    - ssh -o ConnectTimeout=30 $DEPLOY_USER@$DEPLOY_SERVER "echo 'SSH соединение работает'"
    - echo "📁 Передача файлов на сервер..."
    - |
      cat deployment_package.tar.gz | ssh $DEPLOY_USER@$DEPLOY_SERVER "
        cd /opt/www || sudo mkdir -p /opt/www
        sudo tar -xzf -
        echo '✅ Файлы распакованы в /opt/www'
      "
    - echo "🎉 Базовый деплой завершен!"
    - echo "🔧 Для полного запуска выполните на сервере:"
    - echo "   sudo systemctl restart flask-helpdesk"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "main"'
  allow_failure: false

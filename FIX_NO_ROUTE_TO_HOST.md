# Исправление ошибки "No route to host"

## Проблема

В логах приложения появляется ошибка:
```
(2003, "Can't connect to MySQL server on 'helpdesk.teztour.com' ([Errno 113] No route to host)")
```

Это означает, что WSL **не может достучаться до серверов MySQL** через VPN.

## Причины

1. **VPN отключился** в Windows
2. **Метрика WSL не настроена** (приоритет маршрутизации)
3. **Firewall блокирует** подключение
4. **DNS не резолвит** хосты

## Диагностика

### Автоматическая диагностика

```bash
# Сделать скрипт исполняемым
chmod +x diagnose_vpn.sh

# Запустить диагностику
bash diagnose_vpn.sh
```

Скрипт проверит:
- ✅ Доступность VPN сервера
- ✅ Доступность helpdesk.teztour.com
- ✅ Доступность quality.teztour.com
- ✅ Порты MySQL (3306)
- ✅ Маршруты
- ✅ DNS настройки

### Ручная диагностика

```bash
# 1. Проверить VPN
ping -c 3 vpn.teztour.com

# 2. Проверить helpdesk
ping -c 3 helpdesk.teztour.com

# 3. Проверить quality
ping -c 3 quality.teztour.com

# 4. Проверить порт MySQL
timeout 3 bash -c "</dev/tcp/helpdesk.teztour.com/3306" && echo "Порт открыт" || echo "Порт закрыт"
```

## Решения

### Решение 1: Настроить метрику WSL (ВАЖНО!)

Это **самое важное** - без этого WSL не будет использовать VPN маршруты.

В **Windows PowerShell от администратора**:

```powershell
# Проверить текущую метрику
Get-NetIPInterface | Where-Object {$_.InterfaceAlias -Match "vEthernet (WSL)"}

# Установить метрику 6000
Get-NetIPInterface | Where-Object {$_.InterfaceAlias -Match "vEthernet (WSL)"} | Set-NetIPInterface -InterfaceMetric 6000
```

**Что это делает:**
- Устанавливает низкий приоритет для интерфейса WSL
- Позволяет WSL использовать VPN маршруты Windows
- Настройка сохраняется между перезагрузками

### Решение 2: Перезапустить WSL

После настройки метрики перезапустите WSL:

```bash
# В WSL выполнить
exit

# В Windows PowerShell
wsl --shutdown

# Снова открыть WSL
wsl
```

### Решение 3: Проверить VPN подключение

В Windows убедитесь что:
- ✅ Cisco Secure Client **подключен**
- ✅ VPN туннель **активен**
- ✅ Статус показывает "Connected"

### Решение 4: Настроить DNS в WSL

Если DNS не работает:

```bash
# Создать/отредактировать файл wsl.conf
sudo nano /etc/wsl.conf

# Добавить:
[network]
generateResolvConf = false

# Сохранить и выйти (Ctrl+X, Y, Enter)

# Перезапустить WSL
exit
# В Windows PowerShell:
wsl --shutdown
wsl

# Настроить DNS вручную
sudo rm /etc/resolv.conf
sudo bash -c 'echo "nameserver 8.8.8.8" > /etc/resolv.conf'
sudo bash -c 'echo "nameserver 8.8.4.4" >> /etc/resolv.conf'
```

### Решение 5: Добавить хосты вручную (временное решение)

Если DNS не работает, можно добавить IP адреса в `/etc/hosts`:

```bash
# Узнать IP адреса (в Windows с VPN):
# nslookup helpdesk.teztour.com
# nslookup quality.teztour.com

# Добавить в /etc/hosts (замените IP на реальные):
sudo bash -c 'echo "10.0.0.172 helpdesk.teztour.com" >> /etc/hosts'
sudo bash -c 'echo "10.0.0.173 quality.teztour.com" >> /etc/hosts'
```

### Решение 6: Использовать IP вместо хостов

Если ничего не помогает, можно использовать IP адреса напрямую:

```bash
# Узнать IP адреса (в Windows с VPN)
# В PowerShell:
# Resolve-DnsName helpdesk.teztour.com
# Resolve-DnsName quality.teztour.com

# Изменить конфигурацию
nano .env

# Изменить на IP адреса (замените на реальные):
MYSQL_HOST=10.0.0.172
MYSQL_QUALITY_HOST=10.0.0.173
```

## Проверка после исправления

```bash
# 1. Запустить диагностику
bash diagnose_vpn.sh

# 2. Если всё ОК, запустить приложение
bash kill_port_5000.sh
python3 app.py
```

## Порядок действий (пошаговый)

### В Windows (PowerShell от администратора):

```powershell
# 1. Убедиться что VPN подключен
# Проверить статус Cisco Secure Client

# 2. Настроить метрику WSL
Get-NetIPInterface | Where-Object {$_.InterfaceAlias -Match "vEthernet (WSL)"} | Set-NetIPInterface -InterfaceMetric 6000

# 3. Перезапустить WSL
wsl --shutdown
```

### В WSL:

```bash
# 1. Открыть WSL
wsl

# 2. Перейти в проект
cd ~/projects/its.teztour.com

# 3. Запустить диагностику
bash diagnose_vpn.sh

# 4. Если всё ОК - запустить приложение
bash kill_port_5000.sh
python3 app.py
```

## Частые проблемы и решения

### Проблема: "VPN сервер недоступен"

**Причина:** VPN отключен в Windows

**Решение:** 
1. Откройте Cisco Secure Client в Windows
2. Подключитесь к VPN
3. Подождите пока статус станет "Connected"
4. Вернитесь в WSL и повторите диагностику

### Проблема: "Ping работает, но порт 3306 закрыт"

**Причина:** Firewall блокирует порт MySQL

**Решение:**
1. В Windows: Отключите временно Windows Firewall для VPN
2. Или добавьте правило для порта 3306

### Проблема: "DNS не резолвит хосты"

**Причина:** WSL использует неправильные DNS серверы

**Решение:** См. Решение 4 выше (настройка DNS)

### Проблема: После перезагрузки Windows всё ломается

**Причина:** Метрика WSL сбрасывается (редко)

**Решение:** Повторить Решение 1 (настройка метрики)

## Автоматизация

Создайте скрипт для быстрой проверки и запуска:

```bash
# Создать файл start.sh
cat > start.sh << 'EOF'
#!/bin/bash
echo "🔍 Проверка VPN..."
if ! bash diagnose_vpn.sh; then
    echo "❌ VPN недоступен. Подключите VPN и повторите."
    exit 1
fi

echo ""
echo "✅ VPN доступен"
echo ""
echo "🔪 Останавливаем старый процесс..."
bash kill_port_5000.sh

echo ""
echo "🚀 Запускаем приложение..."
python3 app.py
EOF

# Сделать исполняемым
chmod +x start.sh

# Использовать
./start.sh
```

## Мониторинг VPN

Если VPN часто отключается:

```bash
# Создать скрипт мониторинга
cat > monitor_vpn.sh << 'EOF'
#!/bin/bash
while true; do
    if ! ping -c 1 -W 2 vpn.teztour.com > /dev/null 2>&1; then
        echo "$(date): ❌ VPN отключен!" | tee -a vpn_monitor.log
    fi
    sleep 60
done
EOF

# Запустить в фоне
chmod +x monitor_vpn.sh
nohup ./monitor_vpn.sh &
```

## Итоговая проверка

После всех исправлений:

```bash
# 1. Диагностика
bash diagnose_vpn.sh

# Все пункты должны быть ✅

# 2. Запуск приложения
bash kill_port_5000.sh
python3 app.py

# 3. Проверка логов
tail -f app_err.log | grep -E "(ERROR|No route)"

# Не должно быть ошибок "No route to host"
```

## Полезные команды

```bash
# Проверить маршруты
ip route

# Проверить DNS
cat /etc/resolv.conf

# Проверить сетевые интерфейсы
ip addr

# Трассировка маршрута
traceroute helpdesk.teztour.com

# Проверить открытые порты
netstat -tuln | grep 3306
```


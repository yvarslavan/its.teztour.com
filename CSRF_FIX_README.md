# Исправление CSRF проблемы - README

**Дата:** 17 октября 2025
**Версия:** 1.0
**Статус:** ✅ Готово к развертыванию

---

## 🎯 Быстрый старт

### Для администратора сервера:

```bash
# 1. Обновите код на сервере
git pull origin main

# 2. Создайте директорию для сессий
sudo mkdir -p /tmp/flask_sessions && sudo chmod 777 /tmp/flask_sessions

# 3. Перезапустите приложение
sudo systemctl restart flask-helpdesk.service

# 4. Проверьте логи
sudo journalctl -u flask-helpdesk.service -f
```

**Подробнее:** См. файл `QUICK_FIX.md` (5-10 минут)

---

## 📚 Документация

### Основные файлы:

| Файл | Описание | Аудитория |
|------|----------|-----------|
| **QUICK_FIX.md** | Быстрое исправление за 5-10 минут | Администраторы |
| **CSRF_SOLUTION.md** | Полное руководство по решению | Разработчики |
| **DEPLOYMENT_CHECKLIST.md** | Пошаговый чек-лист развертывания | DevOps |
| **CSRF_FIX_SUMMARY.md** | Итоговое резюме изменений | Все |
| **test_csrf_production.py** | Скрипт тестирования | QA / Разработчики |

### Дополнительные файлы:

| Файл | Описание |
|------|----------|
| **cleanup_temp_files.sh** | Очистка временных файлов (Linux/Mac) |
| **cleanup_temp_files.bat** | Очистка временных файлов (Windows) |
| **CSRF_FIX_README.md** | Этот файл |

---

## 🔧 Что было исправлено

### Краткое описание:

1. **SESSION_COOKIE_DOMAIN**: Изменен с `its.tez-tour.com` на `.tez-tour.com` (с точкой)
2. **WTF_CSRF_TIME_LIMIT**: Установлен в `None` (отключено истечение токенов)
3. **WTF_CSRF_SSL_STRICT**: Установлен в `False` (для работы за прокси)
4. **Добавлено детальное логирование** для отладки CSRF

### Файлы с изменениями:

- ✏️ `blog/__init__.py` (строки 88-101, 150-153)
- ✏️ `blog/user/routes.py` (строки 209-241, 1127-1148)

---

## 🧪 Тестирование

### Автоматическое тестирование:

```bash
python test_csrf_production.py
```

### Ручное тестирование:

1. Откройте https://its.tez-tour.com/login
2. Откройте Developer Tools (F12)
3. Попробуйте авторизоваться
4. Проверьте, что нет ошибок "Bad Request"

### API тестирование:

```bash
curl https://its.tez-tour.com/session_debug
```

---

## 📋 Сценарии использования

### Сценарий 1: Новое развертывание

**Цель:** Развернуть исправления на продакшене
**Время:** 10-15 минут
**Документ:** `DEPLOYMENT_CHECKLIST.md`

### Сценарий 2: Быстрое исправление

**Цель:** Быстро исправить проблему на работающем сервере
**Время:** 5-10 минут
**Документ:** `QUICK_FIX.md`

### Сценарий 3: Отладка проблем

**Цель:** Диагностировать проблемы с CSRF
**Время:** 15-30 минут
**Документ:** `CSRF_SOLUTION.md` (раздел "Диагностика")

### Сценарий 4: Разработка/тестирование

**Цель:** Понять как работает решение
**Время:** 30-60 минут
**Документ:** `CSRF_FIX_SUMMARY.md`

---

## 🚀 Процесс развертывания

### Шаг 1: Подготовка

1. Прочитайте `QUICK_FIX.md` или `DEPLOYMENT_CHECKLIST.md`
2. Убедитесь в наличии доступа к серверу
3. Сделайте резервную копию

### Шаг 2: Обновление кода

**Вариант A: Через Git (рекомендуется)**
```bash
git pull origin main
```

**Вариант B: Ручное копирование**
```bash
scp blog/__init__.py user@server:/path/to/blog/
scp blog/user/routes.py user@server:/path/to/blog/user/
```

### Шаг 3: Настройка окружения

```bash
# Проверьте .env.production
cat .env.production

# Создайте директорию для сессий
sudo mkdir -p /tmp/flask_sessions
sudo chmod 777 /tmp/flask_sessions
```

### Шаг 4: Перезапуск

```bash
sudo systemctl restart flask-helpdesk.service
sudo journalctl -u flask-helpdesk.service -f
```

### Шаг 5: Проверка

```bash
# Локально
python test_csrf_production.py

# На сервере
curl https://its.tez-tour.com/session_debug
```

---

## ⚠️ Часто задаваемые вопросы (FAQ)

### Q: Безопасно ли отключать `WTF_CSRF_TIME_LIMIT`?

**A:** Да, это безопасно. CSRF токены привязаны к сессии, которая имеет собственное ограничение времени (24 часа). Отключение `WTF_CSRF_TIME_LIMIT` просто убирает дополнительное ограничение для самого токена.

### Q: Почему нужна точка в начале домена?

**A:** Точка в начале (`.tez-tour.com`) позволяет использовать cookies на всех поддоменах, включая `its.tez-tour.com`, `www.tez-tour.com` и т.д.

### Q: Что делать если не помогло?

**A:**
1. Проверьте логи: `sudo journalctl -u flask-helpdesk.service -n 100`
2. Запустите тест: `python test_csrf_production.py`
3. Проверьте API: `curl https://its.tez-tour.com/session_debug`
4. См. раздел "Диагностика" в `CSRF_SOLUTION.md`

### Q: Как откатить изменения?

**A:**
```bash
git revert HEAD
sudo systemctl restart flask-helpdesk.service
```

### Q: Нужно ли изменять nginx конфигурацию?

**A:** Обычно нет, но убедитесь что есть заголовок `proxy_set_header X-Forwarded-Proto $scheme;`

---

## 🛠️ Дополнительные инструменты

### Очистка временных файлов:

**Linux/Mac:**
```bash
bash cleanup_temp_files.sh
```

**Windows:**
```bash
cleanup_temp_files.bat
```

### Мониторинг логов:

```bash
# В реальном времени
sudo journalctl -u flask-helpdesk.service -f

# Последние 100 строк
sudo journalctl -u flask-helpdesk.service -n 100

# Поиск ошибок
sudo journalctl -u flask-helpdesk.service | grep -i "error\|csrf"
```

### Проверка статуса:

```bash
# Статус сервиса
sudo systemctl status flask-helpdesk.service

# Процессы gunicorn
ps aux | grep gunicorn

# Проверка портов
sudo netstat -tulpn | grep 8000
```

---

## 📞 Поддержка

### Если возникли проблемы:

1. **Проверьте документацию:**
   - `QUICK_FIX.md` - для быстрого решения
   - `CSRF_SOLUTION.md` - для детального анализа
   - `DEPLOYMENT_CHECKLIST.md` - для пошаговой инструкции

2. **Проверьте логи:**
   ```bash
   sudo journalctl -u flask-helpdesk.service -n 100
   ```

3. **Запустите тестирование:**
   ```bash
   python test_csrf_production.py
   ```

4. **Свяжитесь с поддержкой:**
   - Email: help@tez-tour.com
   - Приложите логи и результаты тестов

---

## 📝 Чек-лист перед развертыванием

- [ ] Прочитана документация (`QUICK_FIX.md` или `DEPLOYMENT_CHECKLIST.md`)
- [ ] Есть доступ к серверу через SSH
- [ ] Сделана резервная копия
- [ ] Код обновлен (git pull или ручное копирование)
- [ ] Проверены переменные окружения (.env.production)
- [ ] Создана директория для сессий
- [ ] Приложение перезапущено
- [ ] Проверены логи
- [ ] Запущено тестирование
- [ ] Проверена работа авторизации

---

## 📊 Метрики успеха

### До исправления:
- ❌ Авторизация не работает
- ❌ Ошибка "Bad Request: The CSRF session token is missing"
- ❌ Cookies не устанавливаются

### После исправления:
- ✅ Авторизация работает
- ✅ CSRF токены генерируются и валидируются
- ✅ Cookies устанавливаются корректно
- ✅ Нет ошибок в логах

---

## 🔗 Полезные ссылки

### Внутренняя документация:
- [QUICK_FIX.md](./QUICK_FIX.md) - Быстрое исправление
- [CSRF_SOLUTION.md](./CSRF_SOLUTION.md) - Полное руководство
- [DEPLOYMENT_CHECKLIST.md](./DEPLOYMENT_CHECKLIST.md) - Чек-лист развертывания
- [CSRF_FIX_SUMMARY.md](./CSRF_FIX_SUMMARY.md) - Итоговое резюме

### Внешняя документация:
- [Flask-WTF CSRF Protection](https://flask-wtf.readthedocs.io/en/stable/csrf.html)
- [Flask Session Configuration](https://flask.palletsprojects.com/en/2.3.x/config/#sessions)
- [Cookie Security Best Practices](https://owasp.org/www-community/controls/SecureCookieAttribute)

---

## ✅ Итоговый статус

| Компонент | Статус |
|-----------|--------|
| Анализ проблемы | ✅ Завершен |
| Исправления кода | ✅ Внесены |
| Документация | ✅ Создана |
| Тестовый скрипт | ✅ Создан |
| Готовность к развертыванию | ✅ Да |

**Следующий шаг:** Развертывание на продакшене (см. `QUICK_FIX.md`)

---

**Версия документа:** 1.0
**Дата последнего обновления:** 17 октября 2025
**Автор:** AI Assistant
**Статус:** Готово к использованию

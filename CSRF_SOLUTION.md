# Решение проблемы CSRF на продакшене

## Описание проблемы

После развертывания на сервер https://its.tez-tour.com/ возникает ошибка:
- **Internal Server Error** - при загрузке страницы
- **Bad Request: The CSRF session token is missing** - при попытке авторизации

## Обнаруженные проблемы

1. **Неправильный SESSION_COOKIE_DOMAIN**: Был установлен как `its.tez-tour.com` (без точки), что не работает для поддоменов
2. **Отсутствие WTF_CSRF_TIME_LIMIT**: По умолчанию CSRF токены истекают через 1 час
3. **Строгая проверка SSL**: `WTF_CSRF_SSL_STRICT=True` может вызывать проблемы за прокси/балансировщиком

## Внесенные исправления

### 1. Обновлены настройки CSRF в `blog/__init__.py`

**Изменено:**
```python
SESSION_COOKIE_DOMAIN='.tez-tour.com',  # Поддомены с точкой в начале
WTF_CSRF_TIME_LIMIT=None,  # Отключаем ограничение времени для CSRF токенов
WTF_CSRF_SSL_STRICT=False  # Отключаем строгую проверку SSL для CSRF (если за прокси)
```

**Было:**
```python
SESSION_COOKIE_DOMAIN='its.tez-tour.com',  # БЕЗ точки
# Отсутствовали параметры WTF_CSRF_TIME_LIMIT и WTF_CSRF_SSL_STRICT
```

### 2. Добавлено логирование CSRF

Добавлено детальное логирование состояния CSRF при инициализации приложения:
```python
print(f"🔒 [INIT] CSRF Protection: WTF_CSRF_ENABLED = {app.config.get('WTF_CSRF_ENABLED')}")
print(f"🔒 [INIT] CSRF Time Limit: {app.config.get('WTF_CSRF_TIME_LIMIT')}")
print(f"🔒 [INIT] CSRF SSL Strict: {app.config.get('WTF_CSRF_SSL_STRICT')}")
```

### 3. Добавлена отладка в маршрут `/login`

В `blog/user/routes.py` добавлена детальная отладка для диагностики CSRF:
- Проверка наличия CSRF токена в форме
- Проверка состояния сессии
- Проверка cookies
- Вывод заголовков запроса

### 4. Создан скрипт тестирования

Создан файл `test_csrf_production.py` для тестирования CSRF на продакшене.

## Инструкции по развертыванию

### Шаг 1: Обновление кода на сервере

```bash
# Подключитесь к серверу через SSH
ssh user@server

# Перейдите в директорию проекта
cd /path/to/its.teztour.com

# Получите последние изменения
git pull origin main

# Или скопируйте измененные файлы вручную
```

### Шаг 2: Проверка переменных окружения

Убедитесь, что в `.env.production` установлены правильные значения:

```bash
# Проверьте файл .env.production
cat .env.production

# Должны быть установлены:
WTF_CSRF_ENABLED=True
SESSION_TYPE=filesystem
SESSION_COOKIE_SECURE=True
SESSION_COOKIE_HTTPONLY=True
SESSION_COOKIE_SAMESITE=Lax
SESSION_COOKIE_DOMAIN=.tez-tour.com
SECRET_KEY=<ваш-секретный-ключ>
```

### Шаг 3: Перезапуск приложения

```bash
# Если используется systemd
sudo systemctl restart flask-helpdesk.service

# Или если используется gunicorn напрямую
sudo pkill -HUP gunicorn

# Проверьте статус
sudo systemctl status flask-helpdesk.service

# Или проверьте процессы gunicorn
ps aux | grep gunicorn
```

### Шаг 4: Проверка логов

```bash
# Проверьте логи приложения
sudo journalctl -u flask-helpdesk.service -f

# Ищите строки:
# ✅ [INIT] Продакшен режим активен - настройки безопасности применены
# 🔒 [INIT] CSRF Protection: WTF_CSRF_ENABLED = True
# 🔒 [INIT] CSRF Time Limit: None
# 🔒 [INIT] CSRF SSL Strict: False
```

### Шаг 5: Тестирование

```bash
# На локальной машине запустите скрипт тестирования
python test_csrf_production.py

# Или протестируйте вручную через браузер:
# 1. Откройте https://its.tez-tour.com/login
# 2. Откройте DevTools (F12)
# 3. Проверьте Network -> login (POST запрос)
# 4. Убедитесь, что есть поле csrf_token в Form Data
```

## Диагностика проблем

### Проблема 1: CSRF токен по-прежнему отсутствует

**Причина:** Сессия не создается или cookies не устанавливаются

**Решение:**
```bash
# Проверьте права на директорию сессий
ls -la /tmp/flask_sessions/

# Создайте директорию если отсутствует
sudo mkdir -p /tmp/flask_sessions
sudo chmod 777 /tmp/flask_sessions

# Или измените SESSION_FILE_DIR в .env.production
SESSION_FILE_DIR=/var/lib/flask-sessions
```

### Проблема 2: Bad Request 400

**Причина:** CSRF токен не проходит валидацию

**Решение:**
1. Убедитесь, что `SECRET_KEY` одинаковый на всех воркерах gunicorn
2. Проверьте настройки `SESSION_COOKIE_DOMAIN` - должна быть точка в начале
3. Проверьте, что приложение работает за HTTPS

### Проблема 3: Cookies не устанавливаются

**Причина:** Проблемы с конфигурацией домена или HTTPS

**Решение:**
1. Проверьте, что `SESSION_COOKIE_DOMAIN=.tez-tour.com` (с точкой)
2. Убедитесь, что `SESSION_COOKIE_SECURE=True` только для HTTPS
3. Проверьте настройки nginx/apache для proxy_pass

## Конфигурация nginx (если используется)

Убедитесь, что nginx правильно передает заголовки:

```nginx
location / {
    proxy_pass http://127.0.0.1:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    # Важно для CSRF
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Port $server_port;
}
```

## Конфигурация gunicorn

Рекомендуемые настройки для gunicorn:

```bash
gunicorn --bind 127.0.0.1:8000 \
         --workers 4 \
         --timeout 120 \
         --access-logfile /var/log/gunicorn/access.log \
         --error-logfile /var/log/gunicorn/error.log \
         --log-level info \
         wsgi:app
```

## Тестирование изменений

После внесения изменений выполните:

1. **Локальное тестирование:**
   ```bash
   # Запустите приложение в режиме продакшена локально
   export FLASK_ENV=production
   export DEBUG=False
   python run.py
   ```

2. **Тестирование на сервере:**
   ```bash
   # Запустите скрипт тестирования
   python test_csrf_production.py
   ```

3. **Тестирование через браузер:**
   - Откройте https://its.tez-tour.com/login
   - Откройте Developer Tools (F12)
   - Перейдите на вкладку Application -> Cookies
   - Убедитесь, что установлен cookie `helpdesk_session`
   - Проверьте, что в форме есть скрытое поле `csrf_token`

## Откат изменений (если нужно)

Если изменения вызвали проблемы:

```bash
# Откатите изменения
git revert HEAD

# Или восстановите конкретный файл
git checkout HEAD~1 -- blog/__init__.py
git checkout HEAD~1 -- blog/user/routes.py

# Перезапустите приложение
sudo systemctl restart flask-helpdesk.service
```

## Контрольный список

- [ ] Код обновлен на сервере
- [ ] `.env.production` содержит правильные значения
- [ ] Приложение перезапущено
- [ ] Логи показывают правильные настройки CSRF
- [ ] Скрипт `test_csrf_production.py` выполнен успешно
- [ ] Авторизация работает через браузер
- [ ] Нет ошибок в логах

## Дополнительные ресурсы

- [Flask-WTF CSRF Protection](https://flask-wtf.readthedocs.io/en/stable/csrf.html)
- [Flask Session Configuration](https://flask.palletsprojects.com/en/2.3.x/config/#sessions)
- [Gunicorn Deployment](https://docs.gunicorn.org/en/stable/deploy.html)

## Контакты для поддержки

При возникновении проблем обратитесь:
- Email: help@tez-tour.com
- Разработчик: проверьте логи и используйте скрипт `test_csrf_production.py`

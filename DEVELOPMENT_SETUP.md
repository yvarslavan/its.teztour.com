# Решение проблем с конфигурацией разработки

## Проблемы
1. **Ошибки доступа к базе Redmine** - отсутствие файла `.env.development` для режима разработки
2. **Cursor IDE запускается вместо браузера** - неправильная настройка обработчика протоколов HTTP/HTTPS

## Решение

### ✅ Созданы файлы конфигурации

1. **`env.development`** - файл конфигурации для режима разработки
2. **`.env`** - единый файл конфигурации для всех окружений

### 🔧 Исправление проблемы с Cursor IDE

**Проблема**: При переходе по ссылкам типа `https://helpdesk.teztour.com/issues/266935` запускался Cursor IDE вместо браузера.

**Причина**: Cursor IDE был установлен как обработчик по умолчанию для HTTP/HTTPS протоколов.

**Решение**:
```bash
# Установить Firefox как обработчик по умолчанию для HTTP/HTTPS
xdg-mime default firefox.desktop x-scheme-handler/http
xdg-mime default firefox.desktop x-scheme-handler/https

# Проверить настройки
xdg-mime query default x-scheme-handler/https
```

**Результат**: Теперь ссылки открываются в браузере, а не в Cursor IDE.

### 🚀 Быстрый старт

#### Вариант 1: Запуск приложения
```bash
# Активируйте виртуальное окружение
source venv/bin/activate

# Запустите приложение в режиме разработки
python3 app.py
```

#### Вариант 2: Проверка конфигурации
```bash
# Активируйте виртуальное окружение
source venv/bin/activate

# Проверьте конфигурацию
```

### 🔧 Настройка конфигурации

#### Обновите файл `env.development`:
```bash
# Замените эти значения на реальные:
REDMINE_API_KEY=ваш_реальный_api_ключ
MYSQL_PASSWORD=ваш_пароль_mysql
```

#### Проверьте конфигурацию:
```bash
source venv/bin/activate
```

### 📊 Доступные сервисы

После запуска приложение будет доступно на:
- **Приложение**: http://localhost:5000

### 🛠️ Управление приложением

```bash
# Запуск в режиме разработки

# Проверка конфигурации

# Остановка приложения
# Нажмите Ctrl+C в терминале где запущено приложение
```

### 🔍 Отладка

#### Если приложение не запускается:
```bash
# Проверьте конфигурацию
source venv/bin/activate

# Проверьте зависимости
pip list | grep -E "(flask|dotenv|redminelib)"

# Проверьте логи приложения
tail -f logs/app_dev.log
```

#### Если ссылки все еще открываются в Cursor IDE:
```bash
# Проверьте текущие настройки
xdg-mime query default x-scheme-handler/https

# Если показывает cursor.desktop, исправьте:
xdg-mime default firefox.desktop x-scheme-handler/https
xdg-mime default firefox.desktop x-scheme-handler/http
```

### 📝 Важные замечания

1. **Виртуальное окружение**: Всегда активируйте `venv` перед запуском
2. **Redmine**: Это удаленная база данных MySQL, не локальное приложение
3. **Обработчик протоколов**: Убедитесь, что HTTP/HTTPS ссылки открываются в браузере
4. **Конфигурация**: Используйте файл `env.development` для настроек разработки

### 🎯 Результат

После настройки:
- ✅ Ошибки подключения к Redmine устранены
- ✅ Настроена среда разработки
- ✅ Созданы удобные скрипты для управления
- ✅ Приложение запускается в режиме отладки
- ✅ Ссылки открываются в браузере, а не в Cursor IDE
- ✅ Удалены ненужные Docker файлы

### 📞 Поддержка

При возникновении проблем:
2. Проверьте обработчик протоколов: `xdg-mime query default x-scheme-handler/https`
3. Убедитесь, что виртуальное окружение активировано: `source venv/bin/activate`
4. Проверьте логи приложения: `tail -f logs/app_dev.log`

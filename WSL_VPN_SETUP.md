# Настройка WSL для работы с VPN

## Проблема

При работе в WSL с Cisco Secure Client на Windows возникают ошибки подключения к MySQL:
```
(2013, 'Lost connection to MySQL server during query')
```

**Причина:** Файл `.env` настроен на `127.0.0.1` (порт-прокси Windows), который не работает из WSL.

## Решение

WSL с VPN требует **прямого подключения** к серверам через VPN, а не через порт-прокси Windows.

## Быстрая настройка

### Шаг 1: Убедитесь что VPN подключен

В Windows подключитесь через Cisco Secure Client к VPN.

### Шаг 2: Проверьте доступность серверов из WSL

```bash
# Проверка DNS и доступности
ping -c 3 helpdesk.teztour.com
ping -c 3 quality.teztour.com

# Проверка портов MySQL
nc -zv helpdesk.teztour.com 3306
nc -zv quality.teztour.com 3306
```

Если пинг работает (как в вашем случае с `vpn.teztour.com`), переходите к следующему шагу.

### Шаг 3: Создайте правильную конфигурацию

Запустите скрипт настройки для WSL:

```bash
python3 setup_wsl_config.py
```

Этот скрипт:
- Создаст резервную копию `.env` в `.env.backup` (если файл существует)
- Создаст новый `.env` с правильными настройками для WSL

### Шаг 4: Перезапустите приложение

```bash
# Остановите текущее приложение (Ctrl+C если запущено)
# Запустите заново
python3 app.py
```

## Что изменилось

### Старая конфигурация (не работает в WSL)
```env
MYSQL_HOST=127.0.0.1
MYSQL_QUALITY_HOST=127.0.0.1:3307
```

### Новая конфигурация (работает в WSL с VPN)
```env
MYSQL_HOST=helpdesk.teztour.com
MYSQL_QUALITY_HOST=quality.teztour.com
```

## Почему это работает

1. **Cisco Secure Client** создает VPN туннель в Windows
2. **WSL использует сетевой стек Windows** через виртуальный сетевой адаптер
3. После настройки метрики WSL (`Set-NetIPInterface -InterfaceMetric 6000`) WSL получает доступ к VPN-маршрутам
4. Теперь WSL может напрямую обращаться к `helpdesk.teztour.com` и `quality.teztour.com`

## Различия между конфигурациями

| Конфигурация | MYSQL_HOST | MYSQL_QUALITY_HOST | Когда использовать |
|--------------|------------|-------------------|-------------------|
| `.env.development` | 127.0.0.1 | 127.0.0.1:3307 | Windows (через порт-прокси) |
| `.env.production` | helpdesk.teztour.com | quality.teztour.com | Сервер в корпоративной сети |
| **`.env` (WSL)** | **helpdesk.teztour.com** | **quality.teztour.com** | **WSL с VPN** |

## Устранение проблем

### Проблема: "Name or service not known"

```bash
# Проверьте DNS в WSL
nslookup helpdesk.teztour.com
nslookup quality.teztour.com
```

Если DNS не работает, проверьте `/etc/resolv.conf`:
```bash
cat /etc/resolv.conf
```

### Проблема: Таймаут подключения

```bash
# Проверьте что VPN подключен в Windows
# Проверьте метрику сетевого интерфейса WSL в PowerShell (от администратора):
Get-NetIPInterface | Where-Object {$_.InterfaceAlias -Match "vEthernet (WSL)"}
```

Если метрика не 6000, выполните:
```powershell
Get-NetIPInterface | Where-Object {$_.InterfaceAlias -Match "vEthernet (WSL)"} | Set-NetIPInterface -InterfaceMetric 6000
```

### Проблема: Порт 3306 недоступен

```bash
# Убедитесь что MySQL порт открыт
telnet helpdesk.teztour.com 3306
# Или
timeout 3 bash -c "</dev/tcp/helpdesk.teztour.com/3306" && echo "Port is open"
```

## Автоматическая настройка при каждом запуске

Если вы часто переключаетесь между Windows и WSL, можно автоматизировать выбор конфигурации.

Добавьте в начало `app.py`:

```python
import platform
import subprocess
from pathlib import Path

# Автоматический выбор конфигурации
if 'microsoft' in platform.uname().release.lower():
    # Мы в WSL
    print("🐧 Обнаружена WSL - использую конфигурацию для VPN")
    subprocess.run(['python3', 'setup_wsl_config.py'], check=True)
else:
    # Мы в Windows
    print("🪟 Обнаружена Windows - использую .env.development")
```

## Проверка текущей конфигурации

```bash
# Посмотреть текущие MySQL хосты
grep -E "MYSQL.*HOST" .env

# Должно быть:
# MYSQL_HOST=helpdesk.teztour.com
# MYSQL_QUALITY_HOST=quality.teztour.com
```

## Восстановление из резервной копии

Если что-то пошло не так:

```bash
# Восстановить старую конфигурацию
cp .env.backup .env
```


# Запуск приложения в WSL

## Проблема: Порт 5000 занят

Если при запуске `python3 app.py` вы видите:
```
Address already in use
Port 5000 is in use by another program.
```

## Решение

### Вариант 1: Автоматический скрипт (рекомендуется)

```bash
# Сделать скрипт исполняемым (один раз)
chmod +x kill_port_5000.sh

# Запустить скрипт
bash kill_port_5000.sh

# Запустить приложение
python3 app.py
```

### Вариант 2: Ручной поиск и остановка

```bash
# Найти процесс на порту 5000
lsof -i:5000

# Запомните PID (номер процесса) и остановите его
kill -9 <PID>

# Или остановить все Python процессы с app.py
ps aux | grep "python.*app" | grep -v grep | awk '{print $2}' | xargs kill -9

# Запустить приложение
python3 app.py
```

### Вариант 3: Запуск на другом порту

Если не хотите останавливать старый процесс:

```bash
# Изменить порт в app.py или запустить через Flask CLI
export FLASK_RUN_PORT=5001
flask run --port 5001

# Или напрямую в app.py добавьте параметр port
```

## Проверка что приложение работает правильно

После успешного запуска вы должны увидеть:

```
✅ Загружены переменные окружения из .env (режим: development) [WSL detected]
✅ Используется конфигурация из переменных окружения
============================================================
🚀 FLASK DEVELOPMENT SERVER
============================================================
 * Running on http://127.0.0.1:5000
 * Running on http://0.0.0.0:5000
```

И **НЕ должно быть**:
- ❌ `Address already in use`
- ❌ `Lost connection to MySQL server during query`

## Полная последовательность запуска

```bash
# 1. Перейти в директорию проекта
cd ~/projects/its.teztour.com

# 2. Активировать виртуальное окружение
source venv/bin/activate

# 3. Остановить старый процесс
bash kill_port_5000.sh

# 4. Запустить приложение
python3 app.py
```

## Проверка подключения к MySQL

После запуска откройте браузер и перейдите на:
- http://localhost:5000/users/login

Если страница загружается без ошибок базы данных - всё работает! ✅

## Устранение других проблем

### Если всё ещё ошибки MySQL:

```bash
# Проверить конфигурацию
grep -E "MYSQL.*HOST" .env

# Должно быть:
# MYSQL_HOST=helpdesk.teztour.com
# MYSQL_QUALITY_HOST=quality.teztour.com

# Если неправильно - пересоздать
python3 setup_wsl_config.py
```

### Если VPN отключился:

```bash
# Проверить доступность серверов
ping -c 3 helpdesk.teztour.com
ping -c 3 quality.teztour.com

# Если не доступны - подключите VPN в Windows
# Затем перезапустите приложение
```

### Если WSL не определяется:

Проверьте строку в логах:
```
✅ Загружены переменные окружения из .env (режим: development) [WSL detected]
```

Если нет `[WSL detected]` - проверьте файл `/proc/version`:
```bash
cat /proc/version | grep -i microsoft
```

Должно содержать "microsoft".

## Запуск в фоновом режиме

Если нужно запустить приложение в фоне:

```bash
# Запустить в фоне
nohup python3 app.py > app_output.log 2>&1 &

# Сохранить PID
echo $! > app.pid

# Проверить что запущено
cat app.pid
ps -p $(cat app.pid)

# Остановить приложение
kill $(cat app.pid)
```

## Автоматический запуск

Создайте alias в `~/.bashrc`:

```bash
# Добавить в ~/.bashrc
alias start-its='cd ~/projects/its.teztour.com && bash kill_port_5000.sh && python3 app.py'

# Применить изменения
source ~/.bashrc

# Использовать
start-its
```


# Чек-лист развертывания исправлений CSRF

## Предварительные проверки

- [ ] Убедитесь, что у вас есть доступ к серверу через SSH
- [ ] Сделайте резервную копию текущей версии приложения
- [ ] Убедитесь, что DEBUG=False в продакшене
- [ ] Проверьте, что используется HTTPS

## Развертывание на сервере

### 1. Подключение к серверу

```bash
ssh user@its.tez-tour.com
```

### 2. Переход в директорию проекта

```bash
cd /path/to/its.teztour.com
# Обычно это что-то вроде:
# cd /var/www/its.teztour.com
# или
# cd /home/deploy/its.teztour.com
```

### 3. Резервная копия

```bash
# Создайте резервную копию текущей версии
sudo cp blog/__init__.py blog/__init__.py.backup
sudo cp blog/user/routes.py blog/user/routes.py.backup
sudo cp .env.production .env.production.backup
```

### 4. Обновление кода

**Вариант A: Через Git (рекомендуется)**
```bash
# Сохраните локальные изменения
git stash

# Получите обновления
git pull origin main

# Или если используете другую ветку
git checkout production
git pull origin production
```

**Вариант B: Ручное копирование файлов**
```bash
# С локальной машины скопируйте файлы на сервер
scp blog/__init__.py user@its.tez-tour.com:/path/to/its.teztour.com/blog/
scp blog/user/routes.py user@its.tez-tour.com:/path/to/its.teztour.com/blog/user/
scp test_csrf_production.py user@its.tez-tour.com:/path/to/its.teztour.com/
```

### 5. Проверка переменных окружения

```bash
# Проверьте .env.production
cat .env.production

# Убедитесь в наличии следующих переменных:
# FLASK_ENV=production
# DEBUG=False
# SECRET_KEY=<ваш-секретный-ключ>
# WTF_CSRF_ENABLED=True
# SESSION_TYPE=filesystem
# SESSION_COOKIE_SECURE=True
# SESSION_COOKIE_HTTPONLY=True
# SESSION_COOKIE_SAMESITE=Lax
# SESSION_COOKIE_DOMAIN=.tez-tour.com
```

### 6. Проверка директории сессий

```bash
# Проверьте существование директории сессий
ls -la /tmp/flask_sessions

# Если директория не существует, создайте её
sudo mkdir -p /tmp/flask_sessions
sudo chmod 777 /tmp/flask_sessions

# Или используйте другую директорию
sudo mkdir -p /var/lib/flask-sessions
sudo chown www-data:www-data /var/lib/flask-sessions
sudo chmod 755 /var/lib/flask-sessions
```

### 7. Перезапуск приложения

**Если используется systemd:**
```bash
# Перезапустите сервис
sudo systemctl restart flask-helpdesk.service

# Проверьте статус
sudo systemctl status flask-helpdesk.service

# Если сервис не активен, посмотрите ошибки
sudo journalctl -u flask-helpdesk.service -n 50
```

**Если используется gunicorn напрямую:**
```bash
# Найдите процесс gunicorn
ps aux | grep gunicorn

# Мягкая перезагрузка (без простоя)
sudo pkill -HUP gunicorn

# Или жесткая перезагрузка
sudo pkill -9 gunicorn
# Затем запустите снова:
gunicorn --bind 127.0.0.1:8000 --workers 4 wsgi:app &
```

### 8. Проверка логов

```bash
# Смотрите логи в реальном времени
sudo journalctl -u flask-helpdesk.service -f

# Или если логи в файле
tail -f /var/log/flask-helpdesk/error.log
tail -f /var/log/gunicorn/error.log
```

**Ищите следующие строки в логах:**
```
✅ [INIT] Продакшен режим активен - настройки безопасности применены
🔒 [INIT] CSRF Protection: WTF_CSRF_ENABLED = True
🔒 [INIT] CSRF Time Limit: None
🔒 [INIT] CSRF SSL Strict: False
```

### 9. Тестирование

**A. Запустите скрипт тестирования (с локальной машины):**
```bash
python test_csrf_production.py
```

**B. Проверка через браузер:**
1. Откройте https://its.tez-tour.com/login
2. Откройте Developer Tools (F12)
3. Проверьте:
   - **Application -> Cookies** - должен быть cookie `helpdesk_session`
   - **Network -> login (POST)** - должно быть поле `csrf_token` в Form Data
   - **Console** - не должно быть ошибок

**C. Проверка через curl:**
```bash
# С сервера или локальной машины
curl -v https://its.tez-tour.com/login

# Проверьте наличие Set-Cookie в ответе
```

**D. API эндпоинты для отладки:**
```bash
# Проверка состояния сессии
curl https://its.tez-tour.com/session_debug

# Проверка состояния авторизации
curl https://its.tez-tour.com/auth_status

# Должен вернуть JSON с настройками CSRF и сессии
```

### 10. Проверка работоспособности

- [ ] Страница логина загружается без ошибок
- [ ] В HTML есть скрытое поле `csrf_token`
- [ ] Cookie `helpdesk_session` устанавливается
- [ ] Авторизация работает
- [ ] Нет ошибок "Bad Request"
- [ ] Нет ошибок "CSRF token missing"

## Откат изменений (если что-то пошло не так)

### Быстрый откат

```bash
# Восстановите backup файлы
sudo cp blog/__init__.py.backup blog/__init__.py
sudo cp blog/user/routes.py.backup blog/user/routes.py
sudo cp .env.production.backup .env.production

# Перезапустите приложение
sudo systemctl restart flask-helpdesk.service
```

### Откат через Git

```bash
# Откатите последний коммит
git revert HEAD

# Или откатите к предыдущей версии
git reset --hard HEAD~1

# Перезапустите приложение
sudo systemctl restart flask-helpdesk.service
```

## Дополнительные проверки

### Проверка nginx (если используется)

```bash
# Проверьте конфигурацию nginx
sudo nginx -t

# Посмотрите конфигурацию для вашего сайта
sudo cat /etc/nginx/sites-available/its.tez-tour.com

# Убедитесь в наличии правильных proxy заголовков:
# proxy_set_header Host $host;
# proxy_set_header X-Real-IP $remote_addr;
# proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
# proxy_set_header X-Forwarded-Proto $scheme;
```

### Проверка прав доступа

```bash
# Проверьте владельца файлов
ls -la blog/__init__.py
ls -la blog/user/routes.py

# Должны быть доступны для чтения пользователю, под которым работает приложение
# Обычно это www-data, deploy, или ваш пользователь

# Если нужно, измените права
sudo chown -R www-data:www-data /path/to/its.teztour.com
```

### Проверка SSL сертификата

```bash
# Проверьте срок действия SSL сертификата
openssl s_client -connect its.tez-tour.com:443 -servername its.tez-tour.com

# Или через онлайн сервис
# https://www.ssllabs.com/ssltest/analyze.html?d=its.tez-tour.com
```

## Мониторинг после развертывания

### В течение первых 30 минут:

- [ ] Мониторьте логи на наличие ошибок
- [ ] Проверьте, что пользователи могут авторизоваться
- [ ] Убедитесь, что нет новых ошибок 400/500
- [ ] Проверьте использование ресурсов (CPU, память)

### Команды для мониторинга:

```bash
# Логи в реальном времени
sudo journalctl -u flask-helpdesk.service -f

# Использование ресурсов
htop
# или
top

# Статистика nginx (если используется)
sudo tail -f /var/log/nginx/access.log

# Проверка процессов gunicorn
ps aux | grep gunicorn
```

## Контакты

Если возникли проблемы:
- **Email поддержки:** help@tez-tour.com
- **Telegram:** @support_username
- **Телефон:** +X-XXX-XXX-XXXX

## Полезные команды

```bash
# Просмотр последних 100 строк лога
sudo journalctl -u flask-helpdesk.service -n 100

# Поиск ошибок в логах
sudo journalctl -u flask-helpdesk.service | grep -i "error\|csrf\|bad request"

# Очистка старых сессий
sudo find /tmp/flask_sessions -type f -mtime +7 -delete

# Проверка доступности сервиса
curl -I https://its.tez-tour.com/

# Проверка времени отклика
time curl -s https://its.tez-tour.com/login > /dev/null
```

## Завершение

После успешного развертывания:
- [ ] Обновите документацию
- [ ] Уведомите команду об изменениях
- [ ] Закройте соответствующие тикеты/issues
- [ ] Удалите backup файлы (через неделю)

---
**Дата последнего обновления:** 2025-10-17
**Версия документа:** 1.0
